(ns auth-service.handlers
  (:import (io.oauth.server.api AuthorizationApi)))

(defn authorize-fn [request db oauth]
  "I don't do much yet")

(defn token-fn [request db oauth]
  "I don't do much yet")
